# Research Index

Last updated: 2026-05-11 (post Round 5)

| # | Title | Status | Primary sources | Key conclusion (1 line) |
|---|---|---|---|---|
| 01 | [Prior art: Agentic Engineering book, Overstory, OpenHands](./01-prior-art-jayminwest-overstory-openhands.md) | ✅ complete | 4 AE book chapters (overview + 6 + 9/7 + 10/5); 8 Overstory files (README, CLAUDE.md, STEELMAN.md, types/registry/claude adapter/mail client + 2 dir listings); 4 OpenHands sources (README + 3 dir listings) | Don't vendor — wrong execution models. Adopt the AE book's `Agent = Model + Harness` frame and Overstory's headless `AgentRuntime` interface naming for future Stage-4 extraction. Hold single-agent in v1. Multi-agent / software-factory analysis is out of scope here; handled in a sister repo. |
| 02 | [OAuth refresh forks for claude-code-action](./02-oauth-refresh-forks.md) | ✅ complete | `grll/claude-code-base-action/src/setup-oauth.ts` (151 lines, full read); `grll/claude-code-{action,login}` action.yml + READMEs; `anthropics/claude-code-action` action.yml + issue #727 | One canonical implementation: `grll/claude-code-base-action`'s JIT refresh against `https://console.anthropic.com/v1/oauth/token` with hardcoded `CLIENT_ID=9d1c2…62f5e`. Adapt — don't vendor — into `scripts/refresh_oauth.py` driven by `refresh-oauth.yml` on a 6h cron; reverse the secret-update order so `expires_at` is written last; add Slack alerting + retry. Track `anthropics/claude-code-action#727` for eventual native refresh. |
| 03 | [AE book execution-layer chapters](./03-ae-book-exec-layer.md) | ✅ complete | 6 jayminwest.com subchapters (6/2 harness-stack, 6/4 control-system, 6/5 harness-engineering, 6/6 security-permissions-trust, 8/3 cost-and-latency, 8/4 production-concerns) — all ✅ Full review via fetch issue #13 | Six chapters supply directly applicable vocabulary (six-component stack, four-quadrant guides/sensors, six-step harness-engineering loop, harness-as-security-boundary) and 8 specific DESIGN.md edits. **Notable absence:** chapter 8/3 contains **zero rate-limit-handling content**, validating the resume-loop as novel territory for `agent-runner`. Subscription-auth model inverts the chapter 8/3 cost framing. |
| 04 | [OpenHands SDK git-provider abstraction](./04-openhands-sdk-git-provider.md) | ✅ complete | 9 raw.githubusercontent.com files from `OpenHands/OpenHands` (org renamed) — `service_types.py` (332 lines), `provider.py` (651 lines), GitHub mixins, `enterprise/integrations/manager.py`; SDK README; 1 ❌ on api.github.com tree (rate-limited) | Abstraction relocated, not removed: `openhands/app_server/integrations/`. Canonical Protocol = `GitService` (13 methods); orchestrator = `ProviderHandler`; 6 concrete providers (GitHub, GitLab, Bitbucket, Bitbucket DC, Forgejo, Azure DevOps) via mixin composition over `BaseGitService`. Issue trackers (Jira/Linear/Slack) live behind a *separate* `Manager[ViewT]` ABC — confirms `agent-runner`'s plan to keep `ProviderClient` and `IssueTracker` as unrelated seams. Upgrades report 01's 🟡 GitService row to ✅. |
| 05 | [OpenHands GitHub mixins + Jira manager (deep dive)](./05-openhands-github-mixins-jira.md) | ✅ complete | 4 OpenHands files body-read end-to-end (`repos.py`, `resolver.py`, `provider.py:495-651`, `jira_manager.py`) + 1 supplementary (`jira_types.py`) | Port `get_authenticated_git_url` verbatim when Stage 4 adds a second provider (Azure DevOps + Bitbucket DC URL conventions are non-rederivable from upstream docs). Two corrections to report 04: (a) Jira auth is Keycloak SSO + service-account basic auth, not in-band OAuth; (b) `JiraView` is an `ABC` with class-attribute hints, not a Pydantic model. The `resolver.py` review-thread-from-any-comment-id traversal is the lookup `agent-runner` needs for nested PR-review-thread triggers. |
| 06 | [AE book remaining operational chapters](./06-ae-book-remaining-chapters.md) | ✅ complete | 10 jayminwest.com subchapters (chapter 7 Patterns: index + 1, 2, 4, 5, 6, 7; chapter 8 Practices: 1, 2, 6) all ✅ via fetch issue #17. Plus 7 multi-agent chapters explicitly excluded per scope guardrail. | Ralph Wiggum's *"State persists in commits, not context windows"* (§7/4) is the structural twin of `agent-runner`'s rate-limit-resume loop — resume = fresh `claude -p` + outcome-summary injection, NOT transcript replay. Plan-Build-Review (§7/1) supports a `workflow_depth` AgentConfig knob. Coding-agent §7/5 binary-vs-interpretive signal taxonomy validates the harness's preference for test/lint/compile feedback over file-grep. PLAN.md cluster name was inaccurate — chapter-7 is "Patterns" not "architecture / context engineering." |
| 07 | [OAuth-refresh shipping status watch](./07-oauth-refresh-status-watch.md) | ✅ complete (operational watch) | `anthropics/claude-code-action/main/action.yml`, issue #727, last 10 releases, repo CHANGELOG.md (404) | **Trigger NOT fired.** `action.yml` still has only `claude_code_oauth_token`; #727 open with zero maintainer responses since 2025-12-08; v1.0.111–v1.0.119 release notes contain no auth changes. Keep `scripts/refresh_oauth.py` and re-check on a 4-week cadence (next: ~2026-06-08). |
| 08 | [AE book chapters 4 (Context) + 5 (Tool Use) + 9-10 unread subchapters](./08-ae-book-context-tools.md) | ✅ complete | 22 ✅ jayminwest.com subchapters across chapters 4, 5, 9, 10 via fetch issues #19 (index probe) + #20 (body fetch); 7 ⏭️ multi-agent / out-of-scope | Raschka's *"apparent model quality is frequently context quality in disguise"* (§10/1) inverts the debugging order — first audit target is the harness, not the model. Motivates a Layer-0 outer-harness audit step (AgentConfig load, trigger payload assembly, claude launch args) ahead of report 06's Core-Four diagnostic tree. Two new "adopt verbatim" maxims for LESSONS.md (Pit of Success, Context as Code) and a sharpened LESSONS.md line-format `[<prefix-NNNNN>] helpful=N harmful=M *[YYYY-MM-DD]*:: <assertion>` with category prefixes. Surprises: chapter 5 = `5-tool-use` (not `5-tools`); chapter 9 = "Mental Models"; chapter 10 = "Practitioner Toolkit." |
| 09 | [OpenHands resolver.py GraphQL queries (verbatim)](./09-openhands-graphql-queries.md) | ✅ complete | `openhands/app_server/integrations/github/queries.py` body-read; resolver.py imports cross-checked | The three resolver GraphQL strings live in `queries.py` (NOT the speculated `graphql_queries.py` from report 05 §3); per-thread root preview uses `comments(first: 1)` so the matcher only pays for the one comment it needs; `updatedAt` is NOT requested in Query 3 so the resolver silently falls back to epoch-0 — flagged as a deliberate decision point for any agent-runner port. |
| 10 | [OpenHands provider pagination shapes (GitLab / Bitbucket / Azure DevOps)](./10-openhands-provider-pagination.md) | ✅ complete | 4 OpenHands provider `repos.py` files (GitLab, Bitbucket Cloud, Azure DevOps, Forgejo) + 1 base.py extract (Bitbucket Cloud) + 1 ❌ (`bitbucket_dc/service/repos.py` 404 — directory is `bitbucket_data_center/`, single-file, no pagination) | Four non-GitHub providers do **not** converge on cursor-in-response-body — they split three ways: GitLab + Forgejo reuse GitHub's `Link: rel="next"` header (default), Bitbucket Cloud uses body-field `next` URL (override), Azure DevOps has no cursor and slices client-side (override). Recommended `_paginate(url, max, ...) -> list[dict]` Protocol uses GitHub-style as the default (covers 3/5) and accepts overrides for Bitbucket Cloud + Azure DevOps. |
| 11 | [AE book chapters 1 (Foundations) + 2 (Prompt) + 3 (Model)](./11-ae-book-foundations-models-prompting.md) | ✅ complete | 11 ✅ jayminwest.com subchapters + 1 ⏭️ (multi-model = multi-agent) + 2 ❌ (silent-404 SPA responses to wrong-slug probes — kept as evidence) via fetch issues #21 (silent-404 probe) + #22 (corrected probe) + #23 (body fetch) | Chapters 1/2/3 are ~70% recap of reports 03/06/08 but contain **three net-new contributions**: (1) Rabanser et al. arXiv:2602.16666 (Princeton 2026) — academic backing for "engineer the harness around reliability, not capability"; (2) Twelve Leverage Points hierarchy + dated 2026-04-11 Anti-Patterns by Leverage Level catalog (chapter 1 — names "Isolated Prompting" and "Tool Proliferation" as patterns directly applicable to `agent-runner`); (3) compound-error math `p^n` sharpening the `max_turns` defaults-low rationale. 11 itemized DESIGN.md edit suggestions in the report's §5. **Surprises:** brief inverted chapters 2 and 3 (real slugs are `2-prompt` singular and `3-model` singular); chapter 1 was richer than predicted. |

## Conventions

See `.claude/skills/research-pipeline/SKILL.md` for the methodology. Brief reminders:

- Status legend: ✅ complete · 🟡 partial · 📝 draft · ⏳ blocked-on-fetch · 🗑️ deprecated.
- Every report ends with a *Sources reviewed* table — non-negotiable for auditability.
- See `PLAN.md` for in-flight items and future-research clusters.
